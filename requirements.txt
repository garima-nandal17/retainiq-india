# RetainIQ India — Customer Decision Intelligence Platform
# Target: Python 3.13  (locked project-wide)
#
# These are 3.13-SAFE MINIMUM FLOORS. After a clean install on your machine,
# freeze exact versions:  pip freeze > requirements.lock   (commit the lock).

# --- Core data ---
pandas>=2.2.3          # first pandas with cp313 wheels
numpy>=2.1             # first numpy with cp313 wheels (old 1.26 had none)

# --- SQL engine (local, embedded, reproducible) ---
duckdb>=1.1.0

# --- Modeling (interpretable-first) ---
scikit-learn>=1.5.1
lifelines>=0.30.1      # adds Python 3.13/3.14 support

# --- Explainability ---
shap>=0.48             # first shap with 3.13 support (pulls numba>=0.61)

# --- Stats ---
scipy>=1.14.1
statsmodels>=0.14.4

# --- Dashboard ---
streamlit>=1.40

# --- Viz ---
matplotlib>=3.9.2
seaborn>=0.13.2

# --- Notebooks / testing ---
jupyter>=1.1.1
pytest>=8.2