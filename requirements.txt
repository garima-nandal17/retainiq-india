# RetainIQ India — deployment requirements (Streamlit Community Cloud)
# Pinned to the versions the project is verified against. To reproduce locally,
# these match requirements.lock. Python 3.13 (see runtime.txt).
streamlit==1.59.2
pandas==2.3.3
numpy==2.4.4
duckdb==1.5.4
pyarrow==24.0.0
plotly==6.9.0
scikit-learn==1.8.0
lifelines==0.30.3
shap==0.52.0
statsmodels==0.14.6
scipy==1.17.1
matplotlib==3.10.8
joblib==1.5.3